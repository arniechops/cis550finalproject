{
  "name": "cis550finalproject",
  "version": "1.0.0",
  "description": "Final project for CIS550.",
  "main": "index.js",
  "scripts": {
      "build": "cd frontend && npm run build",
        "install-client": "cd frontend && npm install",
        "heroku-postbuild": "npm run install-client && npm run build",
        "server": "node app.js",
        "start": "cd backend && node app.js && (cd .. && cd frontend && npm start)"
  },
  "repository": {
    "type": "git",
    "url": "cis550finalproject"
  },
  "author": "Arnav Chopra",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mysql": "^2.18.1"
  }
}
